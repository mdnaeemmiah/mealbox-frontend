import LoginPage from '@/components/modules/auth/login/LoginForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <LoginPage></LoginPage>
        </div>
    );
};

export default page;