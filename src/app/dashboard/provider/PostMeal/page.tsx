import CreateMealForm from "@/components/modules/form/provider/CreateMealForm";

const page = () => {
    return (
        <div>
            <CreateMealForm></CreateMealForm>
        </div>
    );
};

export default page;